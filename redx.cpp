#include <iostream>
#include <immintrin.h>



#define GCD(a, v) \

#define AND(a, b) _mm256_and_pd(a, b)
#define  OR(a, b) _mm256_or_si256(a, b)


int main(){

     __m256d B, N, NX, T;

     B, N = AND(B, N);

     return 0;

}
